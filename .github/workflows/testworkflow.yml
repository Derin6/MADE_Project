name: run Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run pipeline.sh
        run: |
          chmod +x project/pipeline.sh
          ./project/pipeline.sh
