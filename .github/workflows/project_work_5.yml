name: Project Feedback

on:
  push:
    branches:
      - main

jobs:
  project-ci:
    runs-on: ubuntu-latest
    steps:
    - name: Set up Node
      uses: actions/setup-node@v3
      with:
        path: main


    - name: Project pipeline
      run: sh test.sh
